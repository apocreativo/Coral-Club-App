# Coral Club App (Golden)
Vite + React. Multiusuario con Vercel KV.

## Pasos
1. `npm i`
2. Crear `.env` con:
```
KV_REST_API_URL=...
KV_REST_API_TOKEN=...
```
3. `npm run dev` (local) / subir a Vercel.

## Claves
- STATE_KEY = coralclub:state
- REV_KEY   = coralclub:rev
